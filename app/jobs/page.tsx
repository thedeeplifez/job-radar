import { AppShell } from "@/components/app-shell";

export default function JobsPage() {
  return (
    <AppShell>
      <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <h2 className="text-2xl font-semibold text-slate-900">Jobs</h2>
        <p className="mt-2 text-sm text-slate-600">Filter and review UAE DevOps and cloud roles. This page will grow into the full search dashboard in later phases.</p>
      </section>
    </AppShell>
  );
}
